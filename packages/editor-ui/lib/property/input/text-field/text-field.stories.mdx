import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

import TextField from "./";
import Icon from "../../../icons";

<Meta title="Property/TextField" component={TextField} />

# TextField

`TextField` Component

<Canvas>
  <Story name="single">
    <div style={{ padding: "2em", backgroundColor: "rgba(0,0,0,0.2)" }}>
      <TextField placeholder="Label" />
    </div>
  </Story>
</Canvas>

# TextField Icon

<Canvas>
  <Story name="test">
    <div style={{ padding: "2em", backgroundColor: "rgba(0,0,0,0.2)" }}>
      <TextField
        leftIcon={<Icon name="hirachyLayout" />}
        placeholder="Label"
        rightIcon={<Icon name="hirachyLayout" />}
      />
    </div>
  </Story>
</Canvas>
